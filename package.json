{
  "name": "next-apollo-prisma-nexus",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -r \"yarn nexus:dev\" \"yarn next dev\"",
    "build": "next build",
    "start": "next start",
    "nexus:dev": "ts-node-dev --transpile-only --no-notify --respawn pages/api/graphql.ts",
    "nexus:build": "tsc",
    "reset-db": "yarn prisma migrate reset",
    "stop-infra": "docker compose down",
    "start-infra": "docker compose up -d",
    "reset-infra": "yarn stop-infra && yarn start-infra && sleep 1 && yarn prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^2.23.0",
    "apollo-server": "^2.25.0",
    "apollo-server-micro": "^2.25.0",
    "graphql": "^15.5.0",
    "next": "10.2.3",
    "nexus": "^1.0.0",
    "nexus-plugin-prisma": "^0.35.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "@types/react": "17.0.8",
    "concurrently": "^6.2.0",
    "prisma": "^2.23.0",
    "ts-node": "^10.0.0",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.3.2"
  }
}
